<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itca.eval_practica_2">

    <application
        android:allowBackup="true"
        android:icon="@drawable/registros"
        android:label="@string/app_name"
        android:roundIcon="@drawable/registros"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eval_Practica_2">
        <activity android:name=".actualizar_borrar"></activity>
        <activity android:name=".registros" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>